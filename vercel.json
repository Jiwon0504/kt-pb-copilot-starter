{
  "version": 2,
  "builds": [
    {
      "src": "src/app.py",
      "use": "@vercel/python",
      "config": {
        "includeFiles": ["data/**", "output/*.json"]
      }
    }
  ],
  "routes": [
    { "src": "/(.*)", "dest": "src/app.py" }
  ]
}
